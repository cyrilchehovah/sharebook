<div class="text-center">
  <%= image_tag object.user.picture, class: 'avatar avatar-large' %>

  <p><%= object.user.first_name%> <%= object.user.last_name%></p>
  <%= link_to 'voir son profil', user_path(object.user), class: 'btn btn-primary'%>

  <%= simple_form_for [object, @booking] do |f| %>
  <%= f.button :submit, "Réservez ce livre", class: "btn btn-primary" %>
<% end %>
